% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embodiedLand.R
\name{embodiedLand}
\alias{embodiedLand}
\title{embodiedLand}
\usage{
embodiedLand(
  gdx,
  file = NULL,
  level = "reg",
  type = "all",
  landType = "all",
  bilateral = FALSE,
  secdToFeed = TRUE,
  reassignLivestock = TRUE
)
}
\arguments{
\item{gdx}{GDX file}

\item{file}{optional file name to write the result with \code{write.magpie}}

\item{level}{regional aggregation level (only "reg" supported)}

\item{type}{"production", "consumption", "trade", or "all" (default)}

\item{landType}{"all" (crop + pasture), "crop", or "past"}

\item{bilateral}{logical; if TRUE return bilateral (exporter.importer) flows}

\item{secdToFeed}{logical; if TRUE (default) move the processed-then-fed share
(e.g. soybean -> oilcake -> feed) from the secd pathway to the feed pathway,
so the Livestock pathway captures all crop products that end up as feed. See
\code{\link{embodiedResourceKastner}}.}

\item{reassignLivestock}{logical; if TRUE (default) move every livestock
product's whole footprint into the feed (Livestock) pathway. See
\code{\link{embodiedResourceKastner}}. A no-op for land (no kli land).}
}
\value{
MAgPIE object in Mha. When bilateral=FALSE: (region, year,
  accounting.pathway.product). When bilateral=TRUE: (exporter.importer, year,
  pathway.product).
}
\description{
Consumption-based (embodied) land footprint using the
  column-normalised Kastner allocation in \code{\link{embodiedResourceKastner}}.
  Unlike \code{\link{embodiedLand}} (which uses production + net-trade and can
  produce local negatives), this distributes each region's actual cropland and
  pasture to consumers, so the consumption footprint is non-negative and
  closes globally to total agricultural land.
}
\seealso{
\code{\link{embodiedResourceKastner}}, \code{\link{embodiedLand}}
}
\author{
David M Chen
}
